<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <config>
    <!-- Matches PackagesRoot in build/base.props. Without this, a packages.config restore in a
         repo with no solution file drops packages next to the project instead. -->
    <add key="repositoryPath" value="packages" />
  </config>
</configuration>
